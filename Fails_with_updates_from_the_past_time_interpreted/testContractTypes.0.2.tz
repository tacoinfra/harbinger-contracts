Storage: sp.TRecord(assetCode = sp.TString, computedPrice = sp.TNat, lastUpdateTime = sp.TTimestamp, numDataPoints = sp.TInt, oracleContract = sp.TAddress, prices = sp.TRecord(first = sp.TInt, last = sp.TInt, saved = sp.TMap(sp.TInt, sp.TNat), sum = sp.TNat).layout((("first", "last"), ("saved", "sum"))), volumes = sp.TRecord(first = sp.TInt, last = sp.TInt, saved = sp.TMap(sp.TInt, sp.TNat), sum = sp.TNat).layout((("first", "last"), ("saved", "sum")))).layout((("assetCode", ("computedPrice", "lastUpdateTime")), (("numDataPoints", "oracleContract"), ("prices", "volumes"))))
Params: sp.TVariant(get = sp.TContract(sp.TNat), update = sp.TBigMap(sp.TString, sp.TPair(sp.TTimestamp, sp.TPair(sp.TTimestamp, sp.TPair(sp.TNat, sp.TPair(sp.TNat, sp.TPair(sp.TNat, sp.TPair(sp.TNat, sp.TNat)))))))).layout(("get", "update"))