Storage: sp.TRecord(assetCodes = sp.TList(sp.TString), assetMap = sp.TBigMap(sp.TString, sp.TRecord(computedPrice = sp.TNat, lastUpdateTime = sp.TTimestamp, prices = sp.TRecord(first = sp.TInt, last = sp.TInt, saved = sp.TMap(sp.TInt, sp.TNat), sum = sp.TNat).layout((("first", "last"), ("saved", "sum"))), volumes = sp.TRecord(first = sp.TInt, last = sp.TInt, saved = sp.TMap(sp.TInt, sp.TNat), sum = sp.TNat).layout((("first", "last"), ("saved", "sum")))).layout((("computedPrice", "lastUpdateTime"), ("prices", "volumes")))), numDataPoints = sp.TInt, oracleContract = sp.TAddress).layout((("assetCodes", "assetMap"), ("numDataPoints", "oracleContract")))
Params: sp.TVariant(get = sp.TPair(sp.TString, sp.TContract(sp.TNat)), update = sp.TBigMap(sp.TString, sp.TPair(sp.TTimestamp, sp.TPair(sp.TTimestamp, sp.TPair(sp.TNat, sp.TPair(sp.TNat, sp.TPair(sp.TNat, sp.TPair(sp.TNat, sp.TNat)))))))).layout(("get", "update"))